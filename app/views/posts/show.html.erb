<h2><%= @post.title %> by <%= @post.user.username %> </h2>
<p><%= @post.content %></p>

<h3>Subs</h3>
<ul>
    <% @post.subs.each do |sub| %>
    <li> <a href="<%=sub_url(sub)%>"><%= sub.title %></a></li>
    <% end %>
</ul> 